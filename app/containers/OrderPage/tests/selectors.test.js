// import { fromJS } from 'immutable';
// import { makeSelectOrderPageDomain } from '../selectors';

// const selector = makeSelectOrderPageDomain();

describe('makeSelectOrderPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
