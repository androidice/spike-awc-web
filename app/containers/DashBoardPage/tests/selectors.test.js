// import { fromJS } from 'immutable';
// import { makeSelectDashBoardPageDomain } from '../selectors';

// const selector = makeSelectDashBoardPageDomain();

describe('makeSelectDashBoardPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
