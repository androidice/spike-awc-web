// import { fromJS } from 'immutable';
// import { makeSelectOrdersPageDomain } from '../selectors';

// const selector = makeSelectOrdersPageDomain();

describe('makeSelectOrdersPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
