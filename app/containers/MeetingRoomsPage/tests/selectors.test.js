// import { fromJS } from 'immutable';
// import { makeSelectMeetingRoomsPageDomain } from '../selectors';

// const selector = makeSelectMeetingRoomsPageDomain();

describe('makeSelectMeetingRoomsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
