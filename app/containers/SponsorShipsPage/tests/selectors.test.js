// import { fromJS } from 'immutable';
// import { makeSelectSponsorShipsPageDomain } from '../selectors';

// const selector = makeSelectSponsorShipsPageDomain();

describe('makeSelectSponsorShipsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
