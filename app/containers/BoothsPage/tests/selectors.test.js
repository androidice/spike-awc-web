// import { fromJS } from 'immutable';
// import { makeSelectBoothsPageDomain } from '../selectors';

// const selector = makeSelectBoothsPageDomain();

describe('makeSelectBoothsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
